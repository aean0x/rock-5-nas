# SOPS Secrets Template
# 1. Copy to secrets.yaml.work
# 2. Fill in your values
# 3. Run ./encrypt

# Generate with: mkpasswd -m SHA-512
user_hashedPassword: "$6$..."

# Tailscale auth key (from https://login.tailscale.com/admin/settings/keys)
tailscale_authKey: "tskey-auth-..."

# WiFi PSK (only needed if enableWifi = true in settings.nix)
# wifi_psk: "your_wifi_password"

# Service-specific secrets are declared in their respective service modules:
# - vpn_wgConf: services/arr-suite.nix + transmission.nix
# - services_transmission_credentials: services/transmission.nix
# - services_caddy_cloudflareToken: services/caddy.nix
